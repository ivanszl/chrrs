CMAKE_MINIMUM_REQUIRED (VERSION 2.6 FATAL_ERROR)

SET(MODULE_NAME chrrs)
PROJECT(${MODULE_NAME})

INCLUDE_DIRECTORIES(
	${CMAKE_SOURCE_DIR}
)
SET(SRC_LIST
	luachrrs.c)

LINK_DIRECTORIES(/usr/lib)
LINK_LIBRARIES(crypt)
LINK_LIBRARIES(lua)
LINK_LIBRARIES(m)
LINK_LIBRARIES(cconv)

ADD_LIBRARY(${MODULE_NAME} SHARED ${SRC_LIST})
